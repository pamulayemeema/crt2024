division of two numbers using if condition

// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int a,b;
   scanf("%d%d",&a,&b);
   if(b==0)
   {
       printf("second number cannot be zero \n");
       b=1;
   }
   int c=a/b;
    printf("%d",c);

    return 0;
}
